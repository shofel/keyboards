# Devenv
.devenv*
devenv.local.nix

# direnv
.direnv

# pre-commit
.pre-commit-config.yaml

# LSP and build artifacts
compile_commands.json
*.compile_commands.json

# QMK build artifacts
*.bin
*.hex
*.uf2
*.elf
*.map
build/

.cache

# Claude Code runtime lock (PID-bearing, machine-local)
.claude/scheduled_tasks.lock
